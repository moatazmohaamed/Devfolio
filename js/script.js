// Typying animation

var typed = new Typed(".auto-type", {
    strings: ["Desginer", "Photographer", "Freelancer", "Developer"],
    typeSpeed: 50,
    backSpeed: 50,
    loop: true,
});

// Counter

$(".count").counterUp({
    delay: 10,
    time: 2000,
});
